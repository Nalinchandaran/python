def rotate_to_sorted(arr): 
    n = len(arr)
    index = 0

    for i in range(n - 1):
        if arr[i] > arr[i + 1]:
            index = i + 1
            break    

    return arr[index:] + arr[:index]
arr=[4,4,6,1,2,3]
print(rotate_to_sorted(arr))